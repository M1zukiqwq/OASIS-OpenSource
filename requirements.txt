# OASIS artifact — Python dependencies.
# Developed and tested with Python 3.9. The exact versions used to produce the
# paper's numbers are pinned below.
numpy==2.0.2
scipy==1.13.1
torch==2.8.0
matplotlib            # only needed to re-render the figures; any recent 3.x works

# System dependencies (NOT installable via pip):
# - PostgreSQL 16.9 — required ONLY for the planner-injection experiments
#   (paper Section 6.2.3 and the sparse sweep) and the TPC-H runtime check
#   (Section 6.2.4). The cluster paths/port the scripts expect are documented in
#   experiments/REPRODUCE.md. Every other experiment is pure Python and needs no
#   database; you can also verify those results from the committed outputs
#   without running anything (see "Verifying without re-running" in REPRODUCE.md).
#
# The trained Stage-1 prior checkpoint is included at
# experiments/oasis_torch/artifacts/ckpt_v3_it3.pt, and the v3 (composite-
# objective) results are produced through experiments/oasis_torch/run_v3.py.
