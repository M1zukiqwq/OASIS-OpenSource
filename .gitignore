__pycache__/
*.pyc
*.pyo
.DS_Store
*.log

# Generated outputs and fetched/derived data (regenerated by the scripts).
# The single curated exception is results/oasis_prior_real/, which holds the
# pre-computed GPU-run CSVs behind the §regime tables (kept so make_regime_tables.py
# reproduces those tables without a GPU retrain — see REPRODUCE.md).
experiments/results/*
!experiments/results/oasis_prior_real/
experiments/data/real/
experiments/data/real_cases/
experiments/data/public_traces/

# LaTeX build leftovers
*.aux
*.bbl
*.blg
*.out
*.synctex.gz

# Virtualenvs
.venv/
venv/

# NOTE: intentionally TRACKED (small, needed to reproduce without a GPU retrain):
#   experiments/oasis_torch/artifacts/*.pt   (trained priors, ~1.5 MB)
#   experiments/regime_data/*.npz            (real-column snapshots, ~72 KB)
#   experiments/results/oasis_prior_real/    (curated §regime result CSVs + tables)
